import React, { Component } from 'react';

import routes from '../routes';

class Main extends Component {
  render() {
    return (
      <main>
       {routes} 
      </main>
    );
  }
}

export default Main;
